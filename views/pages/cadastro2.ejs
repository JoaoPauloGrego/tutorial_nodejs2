<!DOCTYPE html>
<html lang="en">

<% include("../partials/head") %>

    <body>
        Cadastro!
        <main>
            <form action="/cadastro2" method="post" class="formItens">
                <label>
                    Seu nome:
                </label>
                <input type="text" name="username" placeholder="Seu nome" id="username" required>
                <label>
                    Senha:
                </label>
                <!-- ... campos existentes ... -->

                <input type="password" name="password" placeholder="Sua senha" id="password" required>
                <label>
                    Tipo de usuário:
                </label>
                <select name="role" required>
                    <option value="normal">Normal</option>
                    <option value="admin">Admin</option>
                </select>
                <button type="submit">
                    Entrar
                </button>
            </form>

        </main>
        <a href="./index2">Voltar</a>

    </body>
    <style>
        /* Estilos gerais - Mantendo consistência com a página anterior */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        /* Container principal */
        main {
            background-color: white;
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            margin: 20px;
        }

        /* Título */
        body>main::before {
            content: "Cadastro!";
            display: block;
            font-size: 2rem;
            font-weight: bold;
            color: #1a1a1a;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 3px solid #007bff;
            text-align: center;
        }

        /* Formulário */
        .formItens {
            display: grid;
            gap: 1.5rem;
        }

        .formItens label {
            font-weight: 500;
            color: #333;
            margin-bottom: -10px;
            display: block;
        }

        .formItens input {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 6px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .formItens input:focus {
            border-color: #007bff;
            outline: none;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.25);
        }

        /* Botão */
        button[type="submit"] {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        button[type="submit"]:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        /* Link Voltar */
        body>a {
            position: absolute;
            top: 20px;
            left: 20px;
            text-decoration: none;
            color: #007bff;
            font-weight: 500;
            padding: 8px 15px;
            border-radius: 5px;
            transition: all 0.3s ease;
        }

        body>a:hover {
            background-color: #007bff;
            color: white;
            transform: translateY(-2px);
        }

        /* Responsividade */
        @media (max-width: 480px) {
            main {
                padding: 1.5rem;
                margin: 15px;
            }

            body>main::before {
                font-size: 1.75rem;
            }

            .formItens {
                gap: 1rem;
            }

            body>a {
                top: 15px;
                left: 15px;
                padding: 6px 12px;
            }
        }
    </style>

</html>